<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MyComposeApplication">
        <activity
            android:name=".BirthdayCard"
            android:exported="false"
            android:label="@string/title_activity_birthday_card"
            android:theme="@style/Theme.MyComposeApplication" />
        <activity
            android:name=".JetpackComposebasics"
            android:exported="false"
            android:label="@string/title_activity_jetpack_composebasics"
            android:theme="@style/Theme.MyComposeApplication" />
        <activity
            android:name=".ComposableFunctionsActivity"
            android:exported="false"
            android:label="@string/title_activity_composable_functions"
            android:theme="@style/Theme.MyComposeApplication" />
        <activity
            android:name=".MainActivity2"
            android:exported="false"
            android:label="@string/title_activity_main2"
            android:theme="@style/Theme.MyComposeApplication" />
        <activity
            android:name=".DiceRoller.DiceRoller"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.MyComposeApplication.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>